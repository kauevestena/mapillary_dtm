"""
Lightweight VO chain (up-to-scale). Used to assist densification & scale constraints.
"""
from __future__ import annotations

def run(seqs: dict) -> dict:
    """
    Build a frame-to-frame pose chain (R,t up-to-scale) using essential matrix + BA.
    """
    # Placeholder
    return {}
